<%= form_with model: [@merchant, @bulk_discount], local: true do |f| %>
  <%= f.label :threshold %>
  <%= f.text_field :threshold %>
  <%= f.label :percentage %>
  <%= f.number_field :percentage, step: :any %>
  <%= f.submit 'Submit' %>
<% end %>
